import SwiftUI

@main
struct PRLifeMacApp: App {
    var body: some Scene {
        MenuBarExtra("PR Life", systemImage: "waveform") {
            Text("PR Life_")
                .padding()
        }
        .menuBarExtraStyle(.window)
    }
}
